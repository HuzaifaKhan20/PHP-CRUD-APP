1) Create a database 'crudpractice' in MYSQL db in XAMP.
2) Create a table 'crudpractice' with sno as primary key(auto increament), title, description, tstamp (DATETIME) (default value = CURRENTTIMESTAMP)
